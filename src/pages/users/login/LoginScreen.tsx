function LoginScreen(props: {}) {
  // prop destruction
  // lib hooks
  // state, ref hooks
  // form hooks
  // query hooks
  // calculated values
  // effects
  // handlers
  return <>this is login</>;
}

export { LoginScreen };
